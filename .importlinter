[importlinter]
root_packages =
    backend

[importlinter:contract:mixin-independence]
name = Mixins must not import each other
type = independence
modules =
    backend.state
    backend.romm_client
    backend.steam_config
    backend.firmware
    backend.metadata
    backend.sgdb
    backend.downloads
    backend.sync
