{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": [
    "config:recommended"
  ],
  "schedule": [
    "before 6am on monday"
  ],
  "labels": [
    "area:ci"
  ],
  "lockFileMaintenance": {
    "enabled": true,
    "schedule": [
      "before 6am on the first day of the month"
    ]
  },
  "pip_requirements": {
    "enabled": false
  },
  "pip-compile": {
    "managerFilePatterns": [
      "/(^|/)requirements-dev\\.lock$/",
      "/(^|/)requirements-docs\\.lock$/"
    ]
  },
  "packageRules": [
    {
      "description": "Python dev/docs tooling: refresh the uv-compiled lock only WITHIN the declared range. update-lockfile never widens a `<X` ceiling, so deliberate caps (e.g. pytest-asyncio <1.4 for #806) are honored automatically and a ceiling raise stays a manual edit to requirements-*.txt. Auto-merge — the required CI checks are the gate; nothing from pip ships in the plugin.",
      "matchManagers": [
        "pip-compile"
      ],
      "rangeStrategy": "update-lockfile",
      "automerge": true
    },
    {
      "description": "npm + GitHub Actions: auto-merge minor/patch/digest; majors are left for human review.",
      "matchManagers": [
        "npm",
        "github-actions"
      ],
      "matchUpdateTypes": [
        "minor",
        "patch",
        "pin",
        "digest"
      ],
      "automerge": true
    },
    {
      "description": "Toolchain versions (node, pnpm, python, uv, deno) are pinned and cross-file-coupled: each appears in mise.toml AND in package.json's packageManager and/or the workflow setup-* version inputs, and all copies must match (python = Decky's embedded libpython3.11, uv pinned for lock reproducibility, pnpm = packageManager, node = CI). Disabled by NAME across every manager so a bot bump can't desync one copy. The setup-* action SHAs themselves stay updatable (different dep names).",
      "matchDepNames": [
        "node",
        "pnpm",
        "python",
        "uv",
        "deno",
        "astral-sh/uv"
      ],
      "enabled": false
    }
  ],
  "platformAutomerge": true
}
